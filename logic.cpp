#include <iostream>
using namespace std;

int main()
{
   int a = 8;
   int b = 3; 
   int c = a - b;
   cout << (a < b) << ""; 
   cout << (a >= b) << ""; 
   cout << (a >= c) << ""; 
   cout << (a < c) << ""; 
   cout << (a == b) << ""; 
   cout << (a != c) << ""; 

    return 0;
}
